// Top-level build file where you can add configuration options common to all sub-projects/modules.
//plugins {
//alias(libs.plugins.android.application) apply false
//}

// In /build.gradle.kts (your PROJECT's root directory)

// This block defines the plugin versions for all modules in your project.
// 'apply false' means "make this plugin available, but don't apply it to the root project itself."
plugins {
    id("com.android.application") version '8.13.0' apply false // Use a stable version
    id("org.jetbrains.kotlin.android") version "2.2.21" apply false // Use a stable version
}
